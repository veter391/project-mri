{
  "name": "project-mri-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "project-mri — the Next.js marketing site (apps/web) and the Next.js dashboard (apps/dashboard) alongside the Python package in src/",
  "packageManager": "pnpm@11.1.2",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "dev:web": "pnpm --filter @mri/web dev",
    "dev:dashboard": "pnpm --filter @mri/dashboard dev",
    "build:web": "pnpm --filter @mri/web build",
    "build:dashboard": "pnpm --filter @mri/dashboard build",
    "deploy:web:preview": "pnpm --filter @mri/web deploy:preview",
    "deploy:web": "pnpm --filter @mri/web deploy",
    "types:api": "python -m mri.cli openapi -o openapi.json && pnpm --filter @mri/dashboard types:api",
    "typecheck": "pnpm -r --if-present typecheck"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "5.9.2"
  }
}
